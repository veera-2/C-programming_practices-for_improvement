#include <stdio.h>

int main()
{
    // constant = fixed value that cannot be altered by the program during its execution

    const float PI = 3.14159;  // by adding ("const") to the data type, the value that cannot be altered will be fixed

    printf("%.5f\n", PI);


    return 0;
}
